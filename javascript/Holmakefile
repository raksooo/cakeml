INCLUDES = ../misc ../basis/pure ../compiler/parsing ../compiler/inference ../semantics ../semantics/proofs/

CLINE_OPTIONS = --qof

THYFILES = $(patsubst %Script.sml,%Theory.uo,$(wildcard *.sml))
TARGETS = $(patsubst %.sml,%.uo,$(THYFILES))

all: $(TARGETS)

.PHONY: all

